import { Box } from "@mui/material";

const Hr = () => {
  return (
    <Box
      sx={{
        width: "100%",
        height: "2px",
        backgroundColor: "background.paper",
      }}
    />
  );
};

export default Hr;
